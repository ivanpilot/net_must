// export let db: any;

// beforeAll(async () => {
//     db = await db.connectDB(); // connect to DB for test
// });

// beforeEach(async () => {
//     if (db) {
//         await db.cleanDB();
//     }
// });

// afterAll(async () => {
//     await db.disconnectDB();
// });
